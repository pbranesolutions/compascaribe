{
  "name": "compascaribe",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "gsap": "^3.14.2",
    "three": "^0.183.2",
    "vite": "^8.0.8"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.31.2",
    "wrangler": "^4.81.1"
  }
}
